<section id="gwtstructure">
	<title>GWT Project Structure</title>
	<section id="gwtstructure_overview">
		<title>Overview</title>
		<para>If you investigate your example from the last chapter you will
			see the following project structure.</para>
		<para>
			<mediaobject>
				<imageobject>
					<imagedata fileref="images/gwtstructure10.gif" />
					
				</imageobject>
			</mediaobject>
		</para>
		<para>An GWT application consists of the following parts</para>
		<itemizedlist>
			<listitem>
				<para>Module descriptor: XML file which specifies mainly the
					entry
					point of the application. It has the name of your module plus
					.gwt.xml</para>
			</listitem>
			<listitem>
				<para>war: Under the folder "war" you find the HTML page which
					contains the GWT application. Additional public resources, e.g.
					other HTML pages, images or css files should be stored here. Here
					you find also the standard WEB-INF folder.</para>
			</listitem>
			<listitem>
				<para>Source code: under your default package your find a
					package
					"client" All Java code which is compiled for the client
					(Webbrowser) must be stored under client. For the service you would
					use the package "server".</para>
			</listitem>
		</itemizedlist>
	</section>
	<section id="gwtstructure_moduledescriptor">
		<title>Module Descriptor</title>
		<para> The main purpose of the Module descriptor in *.gwt.xml file is
			to define your entry-point. It also defines standard GWT css
			styles which you are using.</para>
		<para>
			<programlisting language="xml">
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="../examples/gwtstructure/De_vogella_gwt_helloworld.gwt.xml" />
			</programlisting>
		</para>
	</section>
</section>