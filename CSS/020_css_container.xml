
<section id="cssusage_div">
	<title>HTML container via id and class</title>
	<para> HTML allows to define sections
		via
		"div" containers. These
		"div"
		containers can be used to style parts
		of
		the HTML document
		differently.
		For this purpose you can identify the div containers via id and class
		attributes.
	</para>

	<para>
		An
		id must be unique in the HTML document while the class
		attribute can
		be defined for several HTML elements. CSS allows to
		style
		these
		elements via special selectors. div can also be identified
		by an
		id
		and / or
		by a class and can container other block elements.
	</para>
	<para>
		The following rule applies:

		<table frame='all'>
			<title>Styling div containers</title>
			<tgroup cols='2' align='left' colsep='1' rowsep='1'>
				<colspec colname='c1' />
				<colspec colname='c2' />
				<thead>
					<row>
						<entry>Definition</entry>
						<entry>CSS selection rule</entry>
					</row>
				</thead>
				<tbody>
					<row>
						<entry><![CDATA[<div id="myid">Content</div> ]]></entry>
						<entry><![CDATA[#myid {css rules....}]]></entry>
					</row>
					<row>
						<entry><![CDATA[<div class="myclass">Content</div> ]]></entry>
						<entry><![CDATA[.myclass {css rules...}]]></entry>
					</row>
				</tbody>
			</tgroup>
		</table>

	</para>

	<para>The
		following example demonstrate both usages. Create the file
		"stylesdiv.css"
	</para>

	<programlisting>
		<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
			parse="text" href="../examples/div/stylesdiv.css" />
	</programlisting>


	<para> Create the following HTML file to use the style sheet.</para>
	<programlisting>
		<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
			parse="text" href="../examples/div/DivTest.html" />
	</programlisting>
	<para>You can also select by position in the HTML document. For
		example
		"td a" only selects links which are within a table row.
	</para>
	<para> You can also use other attributes for example the following
		will
		define certain styling for links which have been visited or over
		which
		the mouse hovers. The will identify if you have a link already
		visited
		or if the mouse hovers over a link and will change the
		display of the
		link accordingly.
	</para>

	<programlisting>
		<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
			parse="text" href="../examples/first/links.css" />
	</programlisting>
</section>
